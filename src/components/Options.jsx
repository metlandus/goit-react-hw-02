import React from 'react'

const Options = (handleClick) => {
    return (
        <>
            <button>Good</button>
            <button>Neutral</button>
            <button>Bad</button>
            <button>Reset</button>
        </>
    )
}

export default Options